// Let keyword
// declare variable with Let keyword

let firstName="Gurkirat";
let firstName="Singh";
console.log(firstName);
// this will give us error that firstName is already been declared when we use "let" but in case of "var" it wil not give any error.

// block scope vs function scope (covered later)